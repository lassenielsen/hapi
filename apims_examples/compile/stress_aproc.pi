#include <console.pi>
#include <timer.pi>
def X(i : Int) =
  if i<=0
  then link(2,timer,t,1);
       t[2]<<^WaitSeconds;
       t[2]<<10;
       t[2]>>ready;
       t[2]<<^End;
       link(2,console,c,1);
       c[2]<<^PutInt;
       c[2]<<SYSTEM&"aprocs";
       c[2]<<^End;
       end
  else ( end
       | X(i-1)
       )
in
link(2,console,c,1);
c[2]<<^PutInt;
c[2]<<SYSTEM&"aprocs";
c[2]<<^End;
X(100)
