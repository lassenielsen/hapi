#include <console.pi>
#include <timer.pi>
local X(Int i)
( if i<=0
  then
  else
  ( t=new timer(2 of 2);
    t[1]<<^WaitSeconds<<i;
    t[1]>>ready;
    t[1]<<^End;
    c=new console(2 of 2);
    c[1]<<^PutInt<<SYSTEM&"aprocs"<<^End;
  | X(i-1);
  )
)
X(10);
